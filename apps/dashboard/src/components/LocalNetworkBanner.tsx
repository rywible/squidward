export function LocalNetworkBanner() {
  return (
    <aside className="local-network-banner" role="note" aria-label="Local network only">
      <strong>Local Network Only</strong>
      <span>This dashboard is intended for trusted LAN access only.</span>
    </aside>
  );
}
